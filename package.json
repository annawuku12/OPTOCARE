{
  "name": "optocare-portal",
  "version": "1.0.0",
  "description": "OPTOCARE UCC Eye Clinic patient portal — static site + Netlify functions",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
